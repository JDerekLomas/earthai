# Publish a tile set: upload to R2, list it in the manifest, verify, deploy (repeatable)

## Posture
Derek zooms into https://earthai-scales.vercel.app/globe/ on any of the ten days and expects the 2.4 km tiles
everywhere GOES sees, not only on 12 Sep. Sets arrive from the Scaleway box one at a time (GOES-East ten-day now,
GOES-West ten-day in ~3 h, then 17-day sets for both after the 30 Aug week is fetched). This job is the same each
time; do it for what is on disk now and leave this file ready for the next one.

## What exists (read before touching anything)
- `.claude/handoffs/2026-09-19-more-days.md` §Report items 3–4 and its addendum: the R2 bucket `earthai-clouds`,
  served cached at `https://clouds.sourcelibrary.org` (30 d edge for streams, 1 h for indexes, so NEVER rebuild a
  set at an existing path: every set gets its own date-keyed prefix), the uploader `scripts/r2_sync.py
  <dir>:<prefix> …` (wrangler, resumable), the manifest's `tiles` list of SETS (each a directory with its own
  `L5.json`/`L4.json`; first listed wins on overlap; `tiles.js` maps the page's frame NAMES to each set's frames,
  so a missing slot such as 2026-09-14T1040Z is simply unmapped).
- `.claude/handoffs/2026-09-18-globe-tiles.md` for how the client and pipeline work; `2026-09-19-tiles-finish.md`
  for the headless-Chrome probe (puppeteer-core at `/Users/dereklomas/node_modules/puppeteer-core`, Chrome at
  `/Applications/Google Chrome.app/Contents/MacOS/Google Chrome`, flags `--use-angle=metal --enable-gpu
  --ignore-gpu-blocklist`, 1440x900 dpr 2; `__setView(lon,lat,dist,frame)`, `__pause()`, `__dbg.tiles.stats`).
- Deploy with `sh scripts/deploy_site.sh` (read its header). The manifest the page reads is `site/globe/clouds.json`
  (check whether the deployed one is generated by a script or edited by hand: `git log -- site/globe/clouds.json`
  and the more-days report say; follow that).

## The set on disk now
`data/clouds/tiles/tenday/goes19/`: `L5/` 112 tiles (1.5 GB, largest 27.8 MB), `L4/` 34 tiles (559 MB), `L5.json`,
`L4.json`. One continuous stream per tile, 1439 frames 2026-09-06T0000Z..2026-09-15T2350Z (2026-09-14T1040Z absent
on S3), codec avc1.640016, gop 12, tv-range, ten-day clear-sky reference. Satellite longitude −75 (GOES-East).

## Do
1. Upload to prefix `globe/tiles_goes19_2026-09-06_2026-09-15/` (both levels and both indexes). Verify a Range
   request on one stream and a GET on `L5.json` from `https://clouds.sourcelibrary.org/...` (206 / 200).
2. Add the set FIRST in the manifest's `tiles`, keep the existing 12 Sep set after it as fallback. Deploy.
3. Verify in headless Chrome on the live page: tiles come on (level 5, `stats.on` reaches 1) over Chile
   (−75, −25) on 8 Sep 19:30Z and on 14 Sep 11:00Z (just after the missing slot), and on 12 Sep as before; note
   tiles-to-sharp ms for each. Confirm the page's coverage sentence now says all ten days over the Americas.
4. Commit (manifest + this file's report; own paths only), push.
5. When `data/clouds/tiles/tenday/goes18/` appears (the box session "hetzner ten-day milestones" messages
   `earthai-46`; ask it if unsure), repeat 1–4 with prefix `globe/tiles_goes18_2026-09-06_2026-09-15/`, satellite
   longitude −137.0 (GOES-West), listed before the 12 Sep set too. Its window straddles the dateline; the index is
   full-width; `tiles.js` already wraps longitude. Check that the shader's per-set satellite longitude (uTileSub)
   is taken from the set, not hardcoded to −75; if it is hardcoded, make it per set (the view-angle fade is wrong
   otherwise) and say so.

## Report back
Append here: prefixes, bytes uploaded, upload rate, the three probe timings, anything that popped. SendMessage
`earthai-46` one line DONE/BLOCKED + URL after each set.
