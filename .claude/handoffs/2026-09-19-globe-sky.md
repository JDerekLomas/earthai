# Globe: a real sky — stars, sun, Moon, and an atmosphere that scatters

## Posture (read first)
Derek opens https://earthai-scales.vercel.app/globe/ on an M5 Pro MacBook (retina, 120 Hz, trackpad), in a dark
room as often as not, and sometimes on a phone. The Earth is already turning with real weather on it. He is a
designer: it has to be beautiful first, and it has to feel like a place seen from orbit, not a textured ball on a
black rectangle. He said, of this stage of the project: "quality and beauty. get on it." He stays a minute or two,
drags, zooms into a storm, lets go, and watches the terminator cross. He leaves if it stutters, spins the fans, or
looks like a screensaver. Everything real on the page is labelled as real in plain sentences below the stage; do
not invent anything that is not labelled.

## What exists (do not redo)
- `site/globe/index.html`: one ShaderMaterial for the planet (Blue Marble day, VIIRS lights at night, cloud from a
  video and, when zoomed, from WebCodecs tiles via `tiles.js`), a separate back-face "atmo" sphere with a cosmetic
  fresnel rim, sun direction per UTC from Spencer's series (`sunVec`), Earth-fixed and Sun-fixed camera modes,
  wall-clock spin, a device-pixel zoom cap. Style tokens are in the page head. Measurement hooks: `window.__dbg`,
  `window.__globe` (perf), `__setView(lon,lat,dist,frame)`, `__pause()`, `__mix`. The cloud pipeline and the tiles
  are NOT yours: do not touch `scripts/`, `tiles.js`, `cloudAt()`, or the cloud uniforms.
- Another session may be adding a cloud-height channel (brief `2026-09-19-cloud-height.md`); it will edit the
  cloud part of the shader and `fetch_clouds.py`. You own the sky, the lighting of the ground and sea, and the
  atmosphere. If both of you need `main()` at once, you pull first and keep your edits in separate functions.
- Measure with puppeteer-core headless Chrome (`/Users/dereklomas/node_modules/puppeteer-core`, executable
  `/Applications/Google Chrome.app/Contents/MacOS/Google Chrome`, flags `--use-angle=metal --enable-gpu
  --ignore-gpu-blocklist`, 1440x900 at dpr 2). Headless fps is not representative (it reads 30 or 60 depending on
  the machine's mood); rAF WORK p95 from `__dbg.rafStats()` is, and it must stay under 4 ms with everything on.
  Serve locally with `npx http-server site -p 8791 -s` (python's server has no Range support; the video will not seek).

## The work, in order of what the eye gets
1. **Real stars.** The Yale Bright Star Catalogue (9,110 stars to magnitude 6.5; public domain; fetch the
   ASCII from a canonical mirror, keep the URL in the report) or Hipparcos to magnitude 7. Render as a point cloud
   (THREE.Points, additive, size from magnitude, colour from B-V through a small lookup: blue-white to orange),
   placed by right ascension / declination on a sphere of radius ~40 around the camera, rotated for the moment
   being shown: Greenwich sidereal time from the frame's UTC, so the sky turns with the day exactly as the Earth
   does (in Sun-fixed mode the stars drift instead). The Milky Way as a faint band: either a lightweight all-sky
   image (ESO's Milky Way panorama is CC BY 4.0; downsample to 2048x1024, credit it in the copy) mapped on the
   same sphere at low opacity, or leave it out and say so. Stars must be subtle: pinpricks, no bloom, dimmed
   where the Earth's limb glow is. Check the geometry with one fact: at 2026-09-12T0400Z the Sun is at RA ~11.3h,
   so Orion should be roughly opposite it; put a named-star check in `__dbg`.
2. **The Sun as a light source you can see.** A lit disc (angular diameter 0.53 deg; do not exaggerate) with a
   soft corona and a faint lens glare that appears when the disc is in the frame, clipped by the Earth. On the
   limb, a sunrise flare where the terminator meets the edge of the disc.
3. **An atmosphere that scatters.** Replace the fresnel rim with a single-scattering model on the back-face
   shell (a handful of samples per fragment along the view ray through a thin shell; Rayleigh only, Mie as a
   small forward term; precompute what you can). Blue by day, a warm band along the terminator that fades to red
   at the very edge, nothing on the night side except a faint airglow line. Also apply it to the ground: aerial
   perspective toward the limb (the ground gets bluer and paler near the edge of the disc) and a warm tint on
   the ground and cloud within ~10 deg of the terminator. Keep one draw call for the planet and one for the shell.
4. **The Moon.** Position from a low-precision lunar ephemeris (Meeus, truncated: ~1 deg is plenty), correct
   phase for the date (12 Sep 2026 is two days past new; check against a published phase), a small textured disc
   (NASA CGI Moon Kit colour map, 2048 wide, public domain) lit by the same sun vector, and moonlight on the
   night side: a faint cool specular on the sea and a slight lift of the night ground and cloud that scales with
   phase. Say in the copy that the Moon is real and its light is computed.
5. **Water and ground at night.** The sea's glint is a single power term. Add a cheap ocean normal perturbation
   (a tiled noise texture, animated slowly with UTC) so the glint breaks up, and use the Blue Marble bathymetry
   already in the basemap to keep shallow water lighter. Night ground: a very faint blue from the sky, not black.
6. **Cloud light, only the two terms that are yours:** a silver lining (forward-scatter brightening on cloud
   within ~15 deg of the terminator, using the opacity gradient toward the sun) and a base-darkening for thick
   cloud (opacity as thickness). Do not change how opacity is read or warped.

## Rules
- Repo: JDerekLomas/earthai, work on main in /Users/dereklomas/sourcelibrary/earthai (data/ is gitignored, so not
  a worktree). Pull first; commit your own paths by name; small commits. No accounts, no keys, no GPU box.
- Every asset gets its licence in the page copy and in the report. Nothing over 2 MB per asset; total added
  bytes to first paint under 300 KB (stars as a compact binary or a short JSON, not the raw catalogue).
- Phones: everything must degrade — the shell's sample count from `devicePixelRatio` and canvas size; stars
  capped at 3,000 on small screens.
- Deploy from main: `pgrep -f vercel` first, then `cd site && npx vercel --prod --yes`. The `site/globe/tiles/`
  directory (260 MB) deploys from disk; do not delete or move it. Curl-check the page after.
- Look at it: screenshots of (a) the whole disc with the sun off-frame, (b) the sunrise limb, (c) the night side
  with Moon and stars, (d) a zoomed terminator crossing cloud, each beside the same view before your change.

## Report back
Append here, numbered and short: what shipped, the URL, the assets and their licences, measured rAF work with
everything on, bytes added, and what you would do next. Then SendMessage "earthai-46" one line DONE/BLOCKED + URL.
